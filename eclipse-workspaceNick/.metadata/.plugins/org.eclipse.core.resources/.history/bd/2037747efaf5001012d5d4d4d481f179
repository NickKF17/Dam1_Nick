package def;

import java.util.Arrays;
import java.util.*;

public class LoshDosh {

	public static void main(String[] args) {
		
		HashSet<Integer> ConjuntoNumeros=new HashSet<>(Arrays.asList(1,44,55,16,17,04));
		ArrayList <Integer> ListaNumeros=new ArrayList<>(List.of(2,23,4,5,6,7,8,4));

		int[] vector = {4,45,5,67,8};

	ArrayList<Integer>vectorLista=new ArrayList<>();
	for(int n:vector)
		vectorLista.add(n);
	System.out.println(vectorLista);
	
	
	HashSet<Integer>vectorConjunto=new HashSet<>();
	for(int n:vector)
		vectorConjunto.add(n);
	System.out.println(vectorConjunto);
	
	int[] vectorListaNumeros = new int[ListaNumeros.size()];
	int i=0;
	for(int n:ListaNumeros) {
		vectorListaNumeros[i]=n;
		i++;
		
	}
	
	for(int n:vectorListaNumeros)
		System.out.print(n+" - ");
	
	
	
	HashSet<Integer> ConjuntoNumeros=new HashSet<>(Arrays.asList(1,44,55,16,17,04));

	int[] vectorConjuntoNumeros = new int[ConjuntoNumeros.size()];
	int j=0
	for(int c:ConjuntoNumeros) {
		vectorConjuntoNumeros[j]=c;
		j++;
		
	}
	
	for(int n:vectorConjuntoNumeros){
		System.out.print(c+" - ");
	}
}
}
