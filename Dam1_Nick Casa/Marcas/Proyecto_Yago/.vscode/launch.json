{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "Abrir HTML actual en Chrome",
            "file": "${file}",
            "webRoot": "${workspaceFolder}"
        }
    ]
}