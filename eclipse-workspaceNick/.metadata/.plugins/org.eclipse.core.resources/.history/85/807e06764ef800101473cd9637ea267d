package def;

import java.util.*;

public class Examen {
    private List<Pregunta> seleccionadas;

    public Examen(int numpregun) {
        List<Pregunta> copia = new ArrayList<>(Pregunta.preguntas);
        
        Collections.shuffle(copia);
        
        int total = Math.min(cantidadAExtraer, copia.size());
        this.seleccionadas = copia.subList(0, total);
    }

    public void mostrar() {
        for (int i = 0; i < seleccionadas.size(); i++) {
            Pregunta p = seleccionadas.get(i);
            System.out.println("\n" + (i + 1) + ". " + p.getEnunciado());
            List<String> opciones = p.getOpcionesMezcladas();
            for (int j = 0; j < opciones.size(); j++) {
                System.out.println("   " + (j + 1) + ") " + opciones.get(j));
            }
        }
    }
}