let resultado = 5 + 3 * 2; // resultado es 11, porque la multiplicación tiene mayor precedencia que la suma
console.log(resultado);

resultado = (5 + 3) * 2; // resultado es 16, porque las operaciones dentro de los paréntesis se evalúan primero
console.log(resultado);